import styles from './ViewBar.module.css';

const ViewBar = () => {
    return (
        <div className={styles.container}>
            <h2>ViewBar</h2>
        </div>
    );
};

export default ViewBar;
